Bare minimum web application built with Flask+Vue.js.

Compile front end asset with:
    $ cd frontend
    $ npm run build

Start service:
    $ cd backend
    $ pipenv run flask --app src run --debug
